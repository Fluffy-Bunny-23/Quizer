{
  "scripts": {
    "test": "cd quiz-app && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "agent-browser": "^0.9.1"
  },
  "dependencies": {
    "playwright": "^1.58.1"
  }
}
